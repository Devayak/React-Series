
 //! All self closing tags need to be close

    //! if we have morethan one same level elements then wrap them in a single container element

    //! class attribute should be written in className ||  for  attribute should be in htmlfor

    //! dom events are written in camelCase eg : onclick->onClick

    //! jsx expression are written inside {} 
    
    const JsxRules=()=>(<div>
      <h1>hello</h1>
    </div>)
    export default JsxRules